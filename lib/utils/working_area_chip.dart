import 'package:flutterBoilerplate/utils/chip.dart';

class WorkingAreaChip extends Chip {
  WorkingAreaChip(
    final int id,
    final String text,
    bool enabled,
  ) : super(
          id,
          text,
          enabled,
        );
}
